apiVersion: apps/v1
kind: Deployment
meta
  name: devops-psu
  labels:
    app: devops-psu
    release-date: RELEASE-DATE  # Это поле будет менять CD пайплайн
  namespace: devops-psu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devops-psu
  template:
    metadata:
      labels:
        app: devops-psu
        svc: frontend
    spec:
      containers:
      - name: devops-psu-server
        image: Bloodflo/devops-psu:latest  # Укажите свой ник!
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
meta
  name: service-devops
  labels:
    app: devops-psu
  namespace: devops-psu
spec:
  type: LoadBalancer
  selector:
    app: devops-psu
    svc: frontend
  ports:
  - port: 12345
    targetPort: 8000
